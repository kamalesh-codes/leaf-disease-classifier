# leaf disease classifier
A leaf disease classifier for 🍓strawberry plants ,where the model is finetuned on a pretrained model from PDDD(Plant Disease Diagnose Dataset).
So using some data augmentation techniques the model is trained on small dataset(5000 samples) which shows significant improvement on both evaluation and inference.
